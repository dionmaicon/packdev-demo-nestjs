{
  "name": "@packdev-demo/notifier",
  "version": "1.0.0",
  "private": true,
  "description": "A small notification service — the second, independently-Dependabot-tracked Nest app in this monorepo, deliberately not a copy of gateway.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/main.js",
    "pretest": "npm run build",
    "test": "node --test dist/test/**/*.test.js"
  },
  "dependencies": {
    "@nestjs/common": "12.0.1",
    "@nestjs/core": "12.0.1",
    "@nestjs/platform-express": "12.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^26.4.0"
  }
}
