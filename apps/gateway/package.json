{
  "name": "@packdev-demo/gateway",
  "version": "1.0.0",
  "private": true,
  "description": "A small orders API — one of two independently-Dependabot-tracked Nest apps in this monorepo.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/main.js",
    "pretest": "npm run build",
    "test": "node --test"
  },
  "dependencies": {
    "@nestjs/common": "11.0.0",
    "@nestjs/core": "11.0.21",
    "@nestjs/platform-express": "11.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
